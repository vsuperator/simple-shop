'use strict';

var React = require('react');
require('../css/product-item.css');

var Basket = React.createClass({
    propTypes: {
        // there will be prop types
    },

    render() {
        return (
            <div>There will be a basket</div>
        );
    }
});


module.exports = Basket;
